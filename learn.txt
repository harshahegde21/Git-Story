<!-- This is to provide Command line functionality  -->
# npm install commander 

<!-- execSync() allows us to run terminal commands from JavaScript. -->

<!-- Why did execFileSync() work?

Because it skips the shell completely.

Instead of:

Node
 ↓
cmd.exe
 ↓
git

it does:

Node
 ↓
git.exe -->